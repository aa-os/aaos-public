# AAOS Contracts

This directory contains governance contracts.

Contracts define admission rules, boundary rules, evidence rules, and fail-closed behavior.

A contract is not an implementation shortcut.

A contract binds external evidence to AAOS governance semantics.
